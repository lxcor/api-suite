"""Django application configuration for the reggi app."""

from django.apps import AppConfig


class ReggiConfig(AppConfig):
    """Application configuration for reggi — identity management and API key issuance."""

    name = 'reggi'
